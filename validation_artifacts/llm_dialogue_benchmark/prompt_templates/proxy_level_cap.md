# Proxy-Level CAP Mode

Answer the user under an external CAP release policy.

Case: `{{case_title}}`

Dialogue:

```text
{{dialogue_context}}
```

Evidence:

```text
{{evidence}}
```

Telemetry:

```text
{{telemetry_tag}}
```

CAP release policy:

```text
{{cap_policy}}
```

The final answer must obey the release policy. Do not defend a deprecated node
or reuse an anchor that requires audit.
