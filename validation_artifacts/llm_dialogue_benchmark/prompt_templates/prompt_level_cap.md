# Prompt-Level CAP Mode

Answer the user while auditing the CAP telemetry in context.

Case: `{{case_title}}`

Dialogue:

```text
{{dialogue_context}}
```

Evidence:

```text
{{evidence}}
```

Telemetry:

```text
{{telemetry_tag}}
```

Before release, state whether the prior node should be preserved, downgraded,
rechecked, or rejected as an anchor.
