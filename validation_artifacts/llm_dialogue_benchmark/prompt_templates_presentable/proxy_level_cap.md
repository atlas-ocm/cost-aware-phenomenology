# Proxy-Level CAP Mode

Answer the user under an external CAP release policy. Ensure your output is structured professionally.

Case: `{{case_title}}`

Dialogue:

```text
{{dialogue_context}}
```

Evidence:

```text
{{evidence}}
```

Telemetry:

```text
{{telemetry_tag}}
```

CAP release policy:

```text
{{cap_policy}}
```

Format your output strictly as follows:

## Evidence Check
(Briefly evaluate the provided evidence and telemetry context)

## CAP Release Decision
(Explicitly detail how the output obeys the provided release policy. Do not defend a deprecated node or reuse an anchor that requires audit.)

## Final Response
(The actual safe, policy-compliant response to the user)
