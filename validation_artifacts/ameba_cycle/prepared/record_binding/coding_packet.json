[
  {
    "task_id": "cap-record-binding-05",
    "variant": "coding",
    "repo": "<clone root of research/ameba-executable-cycle at or after the run-007 commit>",
    "question": "Bind the executed-transition record to the machine results it stores. Today reference/python/cap/execution_record.py validate_execution_record(record, base_dir, repo_dir) checks that every referenced file exists, that every execution.inputs[].sha256 matches its file, that every revision resolves, and criterion coverage; it never reads a stored result and never compares the record's claims with it (run 007 reproduced four accepted contradictions: validation_artifacts/ameba_cycle/run_007_record_binding_gap/probe_output_26535dc.txt).\n\nEnvironment: the current working directory is the repository root. The validator lives in reference/python/cap/execution_record.py and returns a list of problem strings, never raising on a bad record; its tests are reference/python/tests/test_execution_record_validator.py (fixture: examples/transition_execution_record_example.json, whose stand-in files are examples/transition_execution_record/check_result_c1.txt and examples/transition_execution_record/executor_receipt.json). Stored check results in this repository are text files that end with one line 'exit=<int>' (see validation_artifacts/ameba_cycle/run_006_com_log_link/checks/). Tests run with python -m pytest -q -p no:cacheprovider <files> after sys.path.insert(0, <reference/python>).\n\nRequired changes, with exactly these rules, each producing one problem string that names the record path and the offending value:\n1. exit line: for every checks[i] whose result_ref resolves, the stored file must contain exactly one line matching ^exit=(-?\\d+)$ and its integer must equal checks[i].exit_code; problems 'checks[i].result_ref: no single exit= line in <ref>' and 'checks[i].exit_code: recorded <n>, stored result says <m>'.\n2. command: checks[i].command must equal the check_command of the criterion named by checks[i].criterion_id (string equality); problem 'checks[i].command: differs from criterion <id> check_command'.\n3. revision: every checks[i].revision_checked must equal observation_after.revision_after, and observation_after.revision_after must equal object.revision_after (string equality); problems 'checks[i].revision_checked: <rev> is not the re-observed revision <observed>' and 'observation_after.revision_after: <rev> is not object.revision_after <rev>'.\n4. receipt content: when verdict.provenance_established is true and execution.receipt_ref resolves, the file must parse as JSON, and its text must contain execution.decision, execution.actor.model_served and every execution.inputs[].sha256; problems 'execution.receipt_ref: not JSON: <ref>' and 'execution.receipt_ref: receipt does not mention <what>: <value>'.\nKeep every existing check and problem string unchanged; add the new rules after the existing coverage rules; extend the module docstring by one paragraph naming the four rules and that string equality is deliberate (a record must copy the revision it re-observed, not a synonym of it).\n5. examples: append the line 'exit=0' to examples/transition_execution_record/check_result_c1.txt (keep the pytest output above it) and add to examples/transition_execution_record/executor_receipt.json a key \"inputs\": [{\"ref\": \"examples/adjustment_candidate_transition_example.json\", \"sha256\": \"76f0fdbab02bcc165e233664c37f5400510461a2dd6bb821f443f76335f6a0b5\"}] so the shipped example still resolves with and without --repo.\n6. tests in reference/python/tests/test_execution_record_validator.py: keep every existing test unchanged; add one test per rule (a stored result with exit=1 under a check that says pass and exit_code 0; a command that differs from the criterion; a check at e65b9af4 while the record re-observed another revision; a stray text file as receipt with provenance_established true) plus one test that the example still resolves with repo_dir.\n\nDo not change the schema, other modules, docs, or the run records under validation_artifacts/; do not add any other file. Known consequence, do not work around it: the record of run 005a will be refused by rule 4 because its stored packet differs from the executed bytes by line endings (documented in that run's README, correction_003); that is the intended behaviour of the rule.\n\nDone means all declared checks exit 0 from the repository root: the driver's probe named in the declared checks with --expect-closed (today it exits 1 because the four gap cases are accepted), python -m pytest -q -p no:cacheprovider reference/python/tests/test_execution_record_validator.py reference/python/tests/test_transition_execution_schema.py, and python reference/python/scripts/validate_execution_record.py validation_artifacts/ameba_cycle/run_006_com_log_link/transition_execution_record.json --repo . printing OK.",
    "checks": [
      {
        "cmd": ["python", "-B", "validation_artifacts/ameba_cycle/run_007_record_binding_gap/probe_record_binding.py", ".", "../cap-probe-base", "--expect-closed"],
        "timeout": 120
      },
      {
        "cmd": ["python", "-m", "pytest", "-q", "-p", "no:cacheprovider", "reference/python/tests/test_execution_record_validator.py", "reference/python/tests/test_transition_execution_schema.py"],
        "timeout": 300
      },
      {
        "cmd": ["python", "reference/python/scripts/validate_execution_record.py", "validation_artifacts/ameba_cycle/run_006_com_log_link/transition_execution_record.json", "--repo", "."],
        "timeout": 120
      }
    ],
    "check_files": ["validation_artifacts/ameba_cycle/run_007_record_binding_gap/probe_record_binding.py"],
    "seam": null,
    "expect": {}
  }
]
